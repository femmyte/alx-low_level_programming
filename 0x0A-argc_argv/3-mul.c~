#include <stdio.h>

/**
 * main - program that multiplies two numbers.
 * @argc: int
 *@argv: list
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int i;
	int num = 0;

	for( i = 1; i < argc; i++)
	{
		num = num * atoi(argv[i]);
		printf("%d\n", num);
	}

	return (0);
}
